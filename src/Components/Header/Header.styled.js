import styled from 'styled-components';

export const HeaderContainer = styled.div`
  position: relative;
  max-width: 100%;
  left: auto;
  right: auto;
  transform: unset;
  height: auto;
  display: flex;
  -webkit-box-align: center;
  align-items: center;
`;